version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db_scripts/wkping-script.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      POSTGRES_DB: wkping
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

  spring-boot-app:
    image:  wkping-spring-boot-app:v2
    container_name: wkping-bckend-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - postgres

volumes:
  postgres-data:
